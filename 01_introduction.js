/**
 * What is Javascript?
 * Javascipt is a cross-platform , object-oriented scripting language used to make webpages interactive.
 * 
 * Javascript 
 * 1. Object Oriented No distinction between types of objects.Inheritance is through the prototype mechanism, and properties and methods can be added to any object dynamically.
 * 2. Interpreted The code is executed on the client side.
 * 3. Dynamically typed Variables are not declared to have a specific type.
 * 4. Cannot automatically write to the hard drive.
 * 
 * 
 * Java 
 * 1. Class-based . Objects are divided into classes and instances with all inheritance through the class hierarchy.Classes and instances cannot have properties or methods added dynamically.
 * 2. Compiled The code is executed on the server side.
 * 3. Variables data types must be declared.(static type checking)
 * 4. Can write to the hard drive.
 */